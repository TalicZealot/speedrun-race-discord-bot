const { SlashCommandBuilder } = require('@discordjs/builders');

module.exports = {
    name: 'remove',
    async execute(client, race) {
        const data = [];
        //const command = await client.guilds.cache.get('625294110139482113')?.commands.set(data);
        //const result = await client.application?.commands.set(data);
        console.log(result);
    }
};